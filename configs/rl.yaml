il_checkpoint: checkpoints/il/best.pt  # PyTorch state_dict (.pt)
checkpoint_dir: checkpoints/rl/
algorithm: ppo
total_timesteps: 50000
learning_rate: 3.0e-4
n_steps: 2048
batch_size: 64
n_epochs: 10
gamma: 0.99
